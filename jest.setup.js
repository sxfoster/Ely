// Global setup for tests can go here
// For example, you might want to extend expect a
// with custom matchers:
// import '@testing-library/jest-dom/extend-expect';

// If you're using jest-playwright-preset, you might not need much here
// as the preset handles browser setup and teardown.

// Example: Setting up a global variable (use with caution)
// global.MY_GLOBAL_VARIABLE = 'some_value';

// Example: Mocking a module globally
// jest.mock('some-module', () => ({
//   // ...mock implementation
// }));
